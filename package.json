{
  "name": "atproto-app-wizard",
  "version": "1.0.0",
  "description": "AT Protocol App Wizard - Create decentralized web applications",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "npm run build && wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@atproto/api": "^0.19.3",
    "@atproto/oauth-client-browser": "^0.3.41",
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.30.0",
    "@types/node": "^22.10.0",
    "jsdom": "^29.0.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vitest": "^4.0.18",
    "wrangler": "^4.76.0"
  }
}
